// Minimal mock of @holoscript/core for animation package tests.
// Only value exports used by EmotionDirectiveProcessor are needed here;
// all type-only imports are erased at compile time.
export class EmotionDirectiveTrait {}
